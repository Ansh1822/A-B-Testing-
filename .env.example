# Copy this to .env for local overrides, or set these in your host's
# environment variable settings (Vercel / Render / Railway dashboard).

# Leave unset to use local SQLite (./ab_testing.db).
# For production, use a managed Postgres connection string, e.g.:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASE_URL=
